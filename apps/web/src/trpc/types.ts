/**
 * This file re-exports the AppRouter type from the API for use in the frontend.
 * This allows the frontend to have full type safety when calling the API.
 */
export type { AppRouter } from "@api/trpc/app-router.type.ts";
